{
  "schemaVersion": 1,
  "id": "nikostera",
  "version": "${version}",

  "name": "NikosTera",
  "description": "A Server side mod that implements Terastalization",
  "authors": [],
  "contact": {},

  "license": "GPL-3.0",

  "environment": "server",
  "entrypoints": {
    "main": [
      "dev.nxkorasu.nikostera.Nikostera"
    ]
  },

  "mixins": [
    "nikostera.mixins.json"
  ],

  "depends": {
    "fabricloader": ">=${loader_version}",
    "fabric-language-kotlin": ">=${kotlin_loader_version}",
    "fabric": "*",
    "minecraft": "${minecraft_version}"
  }
}
